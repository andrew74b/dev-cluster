apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: andrew74b-cert
  namespace: default
spec:
  commonName: '*.pandathedog.com'
  dnsNames:
    - '*.pandathedog.com'
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: letsencrypt-prod
  secretName: andrew74b-tls
